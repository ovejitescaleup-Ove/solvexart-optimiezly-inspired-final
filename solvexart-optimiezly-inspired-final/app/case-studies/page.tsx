import SiteShell from '../../components/SiteShell'; import {cases as initialCases} from '../../lib/data'; import {useEffect,useState} from 'react';
export default function Cases(){const [cases,setCases]=useState(initialCases);useEffect(()=>{try{const x=JSON.parse(localStorage.getItem('solvexart-cases')||'null');if(Array.isArray(x))setCases(x)}catch{}},[]);return <SiteShell><section className="services-hero"><div className="wrap"><p className="eyebrow">Selected work</p><h1>THE STORY BEHIND THE <em>WORK.</em></h1><p>See how we approach messy marketing problems with clearer strategy, measurement and execution.</p></div></section><section className="section wrap"><div className="case-grid">{cases.map(c=><a key={c.slug} href={'/case-studies/'+c.slug} className="case-card"><span className="eyebrow">{c.industry}</span><h2>{c.title}</h2><p>{c.challenge}</p><span>Read the story →</span></a>)}</div></section></SiteShell>}
