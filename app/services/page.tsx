"use client";
import SiteShell from '../../components/SiteShell'; import {services as initialServices} from '../../lib/data'; import {useEffect,useState} from 'react'; import {ArrowUpRight,ArrowRight,MousePointer2,Search,Globe2,BarChart3,LineChart,BrainCircuit} from 'lucide-react';
const icons=[MousePointer2,Search,Globe2,BarChart3,LineChart,BrainCircuit];
export default function Services(){const [services,setServices]=useState(initialServices);useEffect(()=>{try{const x=JSON.parse(localStorage.getItem('solvexart-services')||'null');if(Array.isArray(x))setServices(x)}catch{}},[]);return <SiteShell><section className="services-hero"><div className="wrap"><p className="eyebrow">Our services</p><h1>THE PIECES OF YOUR MARKETING SHOULD WORK <em>TOGETHER.</em></h1><p>Solvexart combines performance marketing, search, creative, analytics and digital experiences into one practical growth system.</p></div></section><section className="section wrap"><div className="service-list">{services.map((s,i)=>{const Icon=icons[i];return <a href={'/services/'+s.slug} key={s.slug} className="service-row"><div className="service-row-icon"><Icon size={21}/></div><div><span className="eyebrow">0{i+1}</span><h2>{s.title}</h2><p>{s.description}</p></div><ArrowUpRight size={22}/></a>})}</div><div className="center-link"><a className="btn btn-dark" href="/contact">Talk about your goals <ArrowRight size={17}/></a></div></section></SiteShell>}
