import SiteShell from '../../components/SiteShell'; import {posts as initialPosts} from '../../lib/data'; import {useEffect,useState} from 'react';
export default function Blog(){const [posts,setPosts]=useState(initialPosts);useEffect(()=>{try{const x=JSON.parse(localStorage.getItem('solvexart-posts')||'null');if(Array.isArray(x))setPosts(x)}catch{}},[]);return <SiteShell><section className="services-hero"><div className="wrap"><p className="eyebrow">Resources</p><h1>IDEAS TO HELP YOU <em>GROW.</em></h1><p>Practical thinking on paid media, SEO, measurement, conversion and digital growth.</p></div></section><section className="section wrap"><div className="case-grid">{posts.map(p=><a key={p.slug} href={'/blog/'+p.slug} className="case-card"><span className="eyebrow">{p.category} · {p.date}</span><h2>{p.title}</h2><p>{p.excerpt}</p><span>Read article →</span></a>)}</div></section></SiteShell>}
