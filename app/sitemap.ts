import type {MetadataRoute} from 'next'; import {services,cases,posts} from '../lib/data'; export default function sitemap():MetadataRoute.Sitemap{const base='https://solvexart.com';return ['','services','how-we-work','about','case-studies','blog','contact'].map(p=>({url:`${base}/${p}`,lastModified:new Date()})).concat(services.map(s=>({url:`${base}/services/${s.slug}`,lastModified:new Date()})),cases.map(c=>({url:`${base}/case-studies/${c.slug}`,lastModified:new Date()})),posts.map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date()})))}
