'use client';import {useEffect,useState} from 'react';
export default function PostDetailClient({initial}:{initial:any}){const [p,setP]=useState(initial);useEffect(()=>{try{const x=JSON.parse(localStorage.getItem('solvexart-posts')||'null');const f=Array.isArray(x)?x.find((v:any)=>v.slug===initial.slug):null;if(f)setP(f)}catch{}},[initial.slug]);return <article className="wrap py-24 md:py-36 max-w-4xl"><p className="eyebrow">{p.category} · {p.date}</p><h1 className="display text-6xl md:text-8xl font-black mt-5">{p.title}<span className="text-lime">.</span></h1><div className="prose-solvexart mt-14"><p>{p.content}</p><h2>Make decisions from evidence</h2><p>Use clean measurement, focused testing and regular reviews to understand what is actually moving the business forward. The right next step becomes clearer when the customer journey and the data are viewed together.</p></div></article>}
